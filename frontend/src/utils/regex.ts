
const regex = {
  /**
   * ### 비밀번호 규칙
   * - 미정
   */
  password: /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[-`~!@#$%^&*()_+=])[A-Za-z\d-`~!@#$%^&*()_+=]{8,}$/,
  /**
   * ### 이메일 규칙
   * - 미정
   */
  email: /^[a-zA-Z0-9+-_.]+@[a-zA-Z0-9-]+\.[a-zA-Z0-9-.]+$/,
}

export default regex